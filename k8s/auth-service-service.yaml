apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: sre-microservices
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8000
      targetPort: http
