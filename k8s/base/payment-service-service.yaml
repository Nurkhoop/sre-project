apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: sre-microservices
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8000
      targetPort: http
