apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: sre-microservices
spec:
  type: ClusterIP
  selector:
    app: chat-service
  ports:
    - name: http
      port: 8000
      targetPort: http
