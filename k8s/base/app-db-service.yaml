apiVersion: v1
kind: Service
metadata:
  name: app-db
  namespace: sre-microservices
spec:
  type: ClusterIP
  selector:
    app: app-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
