apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: sre-microservices
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - name: http
      port: 8000
      targetPort: http
